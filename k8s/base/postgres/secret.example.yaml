# Copy to secrets.yaml (gitignored) and fill in real credentials before deploying.
# For IONOS, use the template in overlays/ionos/secrets.ionos.example.yaml instead.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: data-platform
type: Opaque
stringData:
  username: hive
  password: CHANGE_ME
